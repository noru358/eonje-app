# 언제 v0.3 — Product Spec

> `언제`는 working brand. 제품 핵심은 이름이 아니라 `질문 → Verdict → Action`이며, 최종 네이밍 확정 전 개발은 중단하지 않는다.

## 0. 제품 문장
**오늘 한강에 언제 가야 가장 좋은지를 한 문장으로 알려준다.**

날씨 앱도, 혼잡도 앱도, 지도 앱도 아니다. 사용자가 여러 지표를 다시 종합하지 않도록 `Data → Verdict → Action`을 완성한다.

## 1. v0 범위
- 장소: 여의도 / 반포 / 뚝섬 / 망원 / 잠실 / 이촌 한강공원
- 질문: `오늘 [장소] 언제 가지?`
- 첫 화면: 결론 1개 + 시간창 1개 + 이유 최대 3개 + 행동 1개
- 상세: 사용자가 `왜 이 시간이야?`를 눌렀을 때만 시간별 근거 노출
- 제외: 지도 탐색, 전국, 커뮤니티, AI 채팅, 복잡한 개인 프로필, 매장 추천

## 2. 핵심 UX 원칙
1. **한 화면 한 판단** — 숫자보다 결론이 먼저다.
2. **Opinionated default** — 가중치 설정을 먼저 요구하지 않는다.
3. **Progressive disclosure** — 상세 데이터는 `왜?` 뒤에 둔다.
4. **Uncertainty without cowardice** — 신뢰도는 표시하되 판단을 회피하지 않는다.
5. **Action last** — 작동하지 않는 알림을 흉내 내지 않는다. v0.3은 실제 동작하는 `캘린더에 넣기`와 `공유`만 제공하고, 추후 PWA Push/이동시간이 준비됐을 때 `출발 알림`을 붙인다.
6. **No fake precision** — 19:17 같은 값 대신 원천 예보 해상도에 맞는 시간창을 준다. v0 기본 표시는 시간 단위이며, 검증 없이 분 단위 정밀도를 만들지 않는다.

## 3. 판정 엔진 v0.3
### Hard gates
- 강한 강수 또는 강수확률 70%+ & 유의미한 강수
- 기상특보
- 33°C 이상 / -5°C 이하
- PM2.5 ≥ 76 또는 PM10 ≥ 151
- 풍속 ≥ 11m/s

### Soft score
- Weather 46%
  - 기온 40%, 강수 40%, 바람 20%
- Crowd 34%
- Experience 20%
- 대기질은 현재값을 **hard gate**로 사용한다. 시간별 예보가 없는 값을 억지로 시간별 점수처럼 쓰지 않는다.
- UV 역시 시간별 신뢰 가능한 입력이 붙기 전까지 추천 시각의 미세 조정에는 쓰지 않는다.
  - 일몰 전후 45분~20분을 최고점
- Event adjustment: v0에서는 소폭 보정만. 데이터 검증 후 독립모델로 분리.

**점수는 내부 산출물이며 첫 화면에 노출하지 않는다.**

## 4. 데이터 계약
서울시 실시간 도시데이터에서 직접 쓸 수 있는 필드:
- 실시간/예측 혼잡: `AREA_CONGEST_LVL`, `FCST_PPLTN`, `FCST_TIME`, `FCST_CONGEST_LVL`
- 현재 환경: `TEMP`, `WIND_SPD`, `PRECIPITATION`, `SUNSET`, `UV_INDEX`, `PM25`, `PM10`
- 24h 예보: `FCST24HOURS[].FCST_DT/TEMP/PRECIPITATION/PRECPT_TYPE/RAIN_CHANCE/SKY_STTS`
- 문화행사: `EVENT_NM`, `EVENT_PERIOD`, `EVENT_PLACE` 등

### 중요한 데이터 리스크
- 서울시는 실시간 원천의 과거 데이터를 별도 제공하지 않는다. **우리 서비스가 운영되기 시작하면 자체 스냅샷 적재가 필요하다.**
- 실시간 인구는 추계값이며 실제 인구와 차이가 날 수 있고 처리 지연이 존재한다.
- 2026-05부터 `SENSIBLE_TEMP`가 삭제됐다. 체감온도는 직접 계산하거나 v0에서 제거한다.
- 통합 `citydata` 공식 문서는 XML을 명시한다. 현재 배포에서 JSON 사용 사례가 있지만, 운영 연결 시 실제 키로 응답 스키마를 먼저 회귀 테스트한다.

## 5. 성공 기준
- **2-second test**: 처음 본 사람이 “한강 갈 시간을 정해주는 서비스”라고 답한다.
- **Decision test**: 결과 뒤에 별도 날씨/혼잡 앱을 확인할 필요를 덜 느낀다.
- **Return test**: 다음 야외 계획 때 다시 연다.
- **Trust test**: 추천이 틀렸을 때 `왜 틀렸는지` 사후 설명이 가능하다.

## 6. 이후로 넘긴 것
- 브라우저 위치 + 이동시간 → `19:05 출발`
- `오늘 어디 한강 가지?` 장소까지 추천
- 산책/러닝/피크닉별 scoring profile
- 실제 알림(PWA Push)
- 사용자 피드백(`딱 맞았음 / 별로였음`)을 통한 가중치 보정


## 7. 데이터 소스 우선순위
1. **서울 실시간 도시데이터**: 장소별 실시간/향후 12시간 혼잡, 일몰, 현재 대기질, 문화행사.
2. **기상청 단기예보**(선택 연결): 1시간 단위 기온·강수확률·강수량·풍속. 서울 통합 API의 미래 풍속 공백을 보완.
3. **자체 스냅샷 저장소**(운영 시작 즉시): 서울 API가 과거 실시간값을 제공하지 않으므로 추천 사후검증과 모델 개선용으로 반드시 적재. v0.3 서버에 `STORE_SNAPSHOTS=1` 옵션으로 5분 버킷 JSONL 적재까지 구현.


## 8. v0.3 웹 인터랙션 계약
- 첫 화면은 장소 + 결론 + 시간창 + 이유 최대 3개.
- score 숫자는 첫 화면에 절대 노출하지 않는다.
- 장소는 query string(`?place=`)으로 공유 가능해야 한다.
- 추천 결과는 서버 `/api/verdict`가 단일 원본이다. 클라이언트가 별도 판정 로직을 갖지 않는다.
- `캘린더에 넣기`는 실제 `.ics` 파일을 생성한다. 구현되지 않은 알림 기능을 성공한 것처럼 표현하지 않는다.
- native share가 가능한 브라우저에서는 Web Share API, 아니면 링크 복사로 fallback한다.
- demo fallback은 반드시 사용자에게 명시한다. 라이브 데이터처럼 위장하지 않는다.
- 오류 시 빈/깨진 카드 대신 재시도 가능한 명시적 error state를 제공한다.
