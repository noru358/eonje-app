import { PLACES } from '/module/places.mjs';
import { applySceneState, seasonFor, daypartFor } from '/scene-system.js';
import { savePlan, isSaved, removeSaved, recordRecent, confirmPlan, clearConfirmed, isConfirmed } from '/storage.js';

const INTENTS=[{id:'general',label:'산책'},{id:'picnic',label:'피크닉'},{id:'run',label:'러닝'},{id:'sunset',label:'노을'}];
const params=new URL(location.href).searchParams;let currentIntent=params.get('intent')||localStorage.getItem('eonje.intent')||'sunset';if(!INTENTS.some(i=>i.id===currentIntent))currentIntent='sunset';let currentPlace=params.get('place')||localStorage.getItem('eonje.place')||'yeouido';if(!PLACES.some(p=>p.id===currentPlace))currentPlace='yeouido';
const markers=new Map();const cache=new Map();const failure=document.getElementById('mapFailure');
if(!globalThis.L){failure.hidden=false;failure.textContent='지도 엔진을 불러오지 못했어요.';throw new Error('Leaflet failed to load')}
const map=L.map('map',{zoomControl:false,attributionControl:true,preferCanvas:true}).setView([37.5288,126.997],12);L.control.zoom({position:'bottomleft'}).addTo(map);const tile=L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'});let tileLoaded=false;tile.on('tileload',()=>{tileLoaded=true;failure.hidden=true});tile.on('tileerror',()=>{if(!tileLoaded)failure.hidden=false});tile.addTo(map);
function scoreOf(v){return Number.isFinite(v?.best?.selectionScore)?v.best.selectionScore:Number.isFinite(v?.best?.score)?v.best.score:-9999}
function markerIcon(place,active=false){return L.divIcon({className:'',html:`<div class="park-marker-wrap ${active?'active':''}"><div class="park-marker-label">${place.shortName}</div><div class="park-marker"></div></div>`,iconSize:[110,58],iconAnchor:[55,54]})}
function setActiveMarker(id){markers.forEach((marker,key)=>{const place=PLACES.find(p=>p.id===key);marker.setIcon(markerIcon(place,key===id))});document.querySelectorAll('.park-pill').forEach(el=>el.classList.toggle('active',el.dataset.id===id))}
function renderIntents(){const root=document.getElementById('intentList');root.innerHTML=INTENTS.map(i=>`<button class="intent-chip ${i.id===currentIntent?'active':''}" data-id="${i.id}" type="button">${i.label}</button>`).join('');root.querySelectorAll('button').forEach(btn=>btn.addEventListener('click',async()=>{currentIntent=btn.dataset.id;localStorage.setItem('eonje.intent',currentIntent);const url=new URL(location.href);url.searchParams.set('intent',currentIntent);history.replaceState(null,'',url);cache.clear();renderIntents();await refreshOverview(true)}))}
async function getVerdict(place){const key=`${place.id}:${currentIntent}`;if(cache.has(key))return cache.get(key);const response=await fetch(`/api/verdict?place=${encodeURIComponent(place.id)}&intent=${encodeURIComponent(currentIntent)}`,{headers:{accept:'application/json'}});if(!response.ok)throw new Error('verdict fetch failed');const payload=await response.json();const result={place,verdict:payload.verdict,data:payload.data};cache.set(key,result);return result}
function planFrom(result){const {place,verdict}=result;const best=verdict?.best||{},when=best.time?new Date(best.time):new Date();return{placeId:place.id,placeName:place.name,signature:place.signature,intent:currentIntent,intentLabel:INTENTS.find(i=>i.id===currentIntent)?.label||currentIntent,windowLabel:verdict.windowLabel||'',headline:verdict.subhead||verdict.headline||'',reasons:(verdict.reasons||[]).slice(0,3),season:seasonFor(when),daypart:daypartFor(when),temp:best.temp,crowd:best.crowd,wind:best.wind,rainChance:best.rainChance}}
function conditionMarkup(best={}){const v=[];if(Number.isFinite(best.temp))v.push(['기온',`${Math.round(best.temp)}°`]);if(best.crowd)v.push(['혼잡',best.crowd]);if(Number.isFinite(best.wind))v.push(['바람',`${best.wind.toFixed(1)}m/s`]);if(Number.isFinite(best.rainChance))v.push(['비',`${Math.round(best.rainChance)}%`]);return v.map(([k,x])=>`<div><span>${k}</span><b>${x}</b></div>`).join('')}
function cautions(v={}){const b=v.best||{},out=[];if(['약간 붐빔','붐빔'].includes(b.crowd))out.push(`사람 ${b.crowd}`);if(Number.isFinite(b.wind)&&b.wind>=5)out.push(`바람 ${b.wind.toFixed(1)}m/s`);if(Number.isFinite(b.rainChance)&&b.rainChance>30)out.push(`비 ${Math.round(b.rainChance)}%`);if(Number.isFinite(b.temp)&&b.temp>=28)out.push(`${Math.round(b.temp)}°로 더움`);if(Number.isFinite(b.temp)&&b.temp<=16)out.push(`${Math.round(b.temp)}°로 쌀쌀`);return out.slice(0,2)}
function evidenceMarkup(v){const good=(v.reasons||[]).slice(0,2).map(r=>`<li><b>${r.title}</b><span>${r.detail||''}</span></li>`).join('')||'<li><b>전체 조건이 균형적</b><span>현재 비교에서 상위예요.</span></li>';const bad=cautions(v);return `<div class="map-evidence"><section><strong>좋아요</strong><ul>${good}</ul></section><section><strong>아쉬워요</strong><ul>${bad.length?bad.map(x=>`<li><b>${x}</b></li>`).join(''):'<li><b>큰 약점 없음</b><span>현재 데이터 기준</span></li>'}</ul></section></div>`}
function panelMarkup(result){const {place,verdict}=result;const time=verdict.windowLabel||(verdict.status==='avoid'?'오늘은 패스':'오늘 추천 종료');const plan=planFrom(result),saved=isSaved(plan),confirmed=isConfirmed(plan);return `<span class="recommend-chip">${verdict.status==='go'?'현재 선택':'확인'}</span><h2>${place.name}</h2><p class="panel-signature">${place.signature||''}</p><p>${verdict.headline||verdict.subhead||'오늘 조건을 비교했어요.'}</p><div class="panel-time-wrap"><div class="panel-time-label">가장 좋은 시간</div><div class="panel-time"><strong>${time}</strong><span>${INTENTS.find(i=>i.id===currentIntent)?.label||''}</span></div></div><div class="map-conditions">${conditionMarkup(verdict.best)}</div>${evidenceMarkup(verdict)}<div class="panel-actions"><button type="button" data-confirm>${confirmed?'정했어요 · 취소':'이 시간으로 정하기'}</button><button type="button" data-save>${saved?'저장됨 · 취소':'저장'}</button><a class="panel-action" href="/?intent=${encodeURIComponent(currentIntent)}">홈에서 보기</a></div>`}
function bindPanelActions(result){const save=document.querySelector('#parkPanel [data-save]'),confirm=document.querySelector('#parkPanel [data-confirm]');if(save)save.addEventListener('click',()=>{const plan=planFrom(result);if(isSaved(plan))removeSaved(plan);else savePlan(plan);save.textContent=isSaved(plan)?'저장됨 · 취소':'저장'});if(confirm)confirm.addEventListener('click',()=>{const plan=planFrom(result);if(isConfirmed(plan))clearConfirmed();else confirmPlan(plan);confirm.textContent=isConfirmed(plan)?'정했어요 · 취소':'이 시간으로 정하기'})}
function renderStrip(results){const root=document.getElementById('parkStrip');const sorted=[...results].sort((a,b)=>scoreOf(b.verdict)-scoreOf(a.verdict));const current=sorted.find(r=>r.place.id===currentPlace);const choices=[current,...sorted.filter(r=>r.place.id!==currentPlace)].filter(Boolean).slice(0,3);root.innerHTML=choices.map(r=>`<button type="button" class="park-pill liquid-glass ${r.place.id===currentPlace?'active':''}" data-id="${r.place.id}" data-place="${r.place.id}"><strong>${r.place.name}</strong><span>${r.place.signature||''}</span><span>${r.verdict.windowLabel||'오늘 조건 보기'}</span></button>`).join('');root.querySelectorAll('button').forEach(btn=>{const result=choices.find(r=>r.place.id===btn.dataset.id);const d=result?.verdict?.best?.time?new Date(result.verdict.best.time):new Date();applySceneState(btn,btn.dataset.id,d);btn.addEventListener('click',()=>selectPark(btn.dataset.id))})}
async function selectPark(id,{pan=true,refreshStrip=true}={}){const place=PLACES.find(p=>p.id===id);if(!place)return;currentPlace=id;localStorage.setItem('eonje.place',id);const url=new URL(location.href);url.searchParams.set('place',id);url.searchParams.set('intent',currentIntent);history.replaceState(null,'',url);setActiveMarker(id);if(pan)map.panTo([place.lat,place.lon],{animate:true});const panel=document.getElementById('parkPanel');panel.innerHTML=`<span class="recommend-chip">확인 중</span><h2>${place.name}</h2><p>${place.signature||'조건을 비교하고 있어요.'}</p>`;try{const result=await getVerdict(place);panel.innerHTML=panelMarkup(result);bindPanelActions(result);recordRecent(planFrom(result));const when=result.verdict?.best?.time?new Date(result.verdict.best.time):new Date();applySceneState(document.body,place.id,when);if(refreshStrip){const results=(await Promise.allSettled(PLACES.map(getVerdict))).filter(x=>x.status==='fulfilled').map(x=>x.value);renderStrip(results)}}catch{panel.innerHTML=`<span class="recommend-chip">오류</span><h2>${place.name}</h2><p>추천 데이터는 불러오지 못했지만 지도 위치는 그대로 확인할 수 있어요.</p>`}}
async function refreshOverview(selectBest=false){const settled=await Promise.allSettled(PLACES.map(getVerdict));const results=settled.filter(x=>x.status==='fulfilled').map(x=>x.value);if(!results.length){await selectPark(currentPlace,{refreshStrip:false});return}if(selectBest){const go=results.filter(r=>r.verdict?.status==='go');const winner=[...(go.length?go:results)].sort((a,b)=>scoreOf(b.verdict)-scoreOf(a.verdict))[0];if(winner)currentPlace=winner.place.id}renderStrip(results);await selectPark(currentPlace,{pan:false,refreshStrip:false})}
PLACES.forEach(place=>{const marker=L.marker([place.lat,place.lon],{icon:markerIcon(place,place.id===currentPlace),title:place.name}).addTo(map);marker.on('click',()=>selectPark(place.id,{pan:false}));markers.set(place.id,marker)});const bounds=L.latLngBounds(PLACES.map(p=>[p.lat,p.lon]));map.fitBounds(bounds.pad(.1),{padding:[34,34]});renderIntents();refreshOverview(!params.get('place'));setTimeout(()=>map.invalidateSize(),50);
